[Zależność] python3
[Zależność] js

type MyResult = Result[int, string]
type MyOption = Option[string]
trait Printable {
    print: () -> string;
}

let mut value = 5
#python {
result = value * 2
}

match value {
    0 -> "Zero"
    1..10 -> "Small"
    _ -> "Other"
} |> [Output] > | print

[If] > result == 10 | [Output] > "Python computed correctly" | print

[Loop] > 3 | [Output] > "Iteration $INDEX" | print

spawn async_func()
try risky_op()?

[Output] > "Advanced Velvet done" | print
