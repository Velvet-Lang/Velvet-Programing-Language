[Library-example] python3 @ Python dependency

let x = 5 @ Type inferred as int
let mut y: string = "Hello" @ Explicit type, mutable

#python {
print("Python says: " + str(x))
}

[Output] > y |> "World!" | print @ Pipe operator
